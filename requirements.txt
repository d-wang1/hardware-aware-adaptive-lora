# Windows-only: importing torch before datasets segfaults when
# pyarrow>=15 is installed. The numpy<2 + pyarrow<15 + datasets<3
# triple is the working intersection. Mac/Linux tolerate it fine.

torch>=2.2
transformers>=4.40
datasets>=2.18,<3
peft>=0.10
accelerate>=0.29
numpy>=1.26,<2
pyarrow<15
pandas>=2.2
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
scikit-learn>=1.4
pytest>=8.0
